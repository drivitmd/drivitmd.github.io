_preview/
fotos/
node_modules/
.DS_Store
Thumbs.db

# never commit secrets
*token*.txt
*Token*.txt
*.token
gh.token.txt
*.key.txt

_ads.html
/ads/
LAUNCH-LOOP.md
_hostphotos/

# og render intermediate (only og.jpg is served)
assets/og.png

# internal render templates & drafts — keep on disk, never serve on the site
_*.html

# superseded design experiments — kept local, not published
v1-antara.html
v2-yin.html
variant-day.html
variant-night.html

# internal specs / design notes
docs/

# stray source screenshot in repo root (keep local, never serve)
/new1.PNG

# build-скрипты: держим на диске, не раздаём с сайта
/launch.py
/build_jurnal.py
/update_facts.py
/retitle_next.py
/ogcard_gen.mjs
