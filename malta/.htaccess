# Частная страница: из поиска исключена на уровне заголовка, это сильнее robots.txt
<IfModule mod_headers.c>
  Header set X-Robots-Tag "noindex, nofollow, noarchive, nosnippet"
  Header set Referrer-Policy "no-referrer"
</IfModule>
Options -Indexes
